# Progress

## 2026-04-19
- 初始化分析记录文件
- 已确认需要重点阅读后端图编排、节点职责、前端调用入口和方案文档
- 已确认真实运行入口、LangGraph builder、TripState 和 API SSE 包装逻辑
- 已确认前端 agent 面板是固定定义，不是动态图可视化
- 已运行最小后端测试：`test_fan_in` 通过，`test_graph_builder` 因并行节点数量断言过期而失败
- 已确认方案文档、LangGraph 配置、实际 builder、SSE 展示顺序之间存在多处漂移

## 2026-05-01
- 用户确认开始修复，并追加前端页面、日期组件、状态图和自动跳转问题
- 已重写 `task_plan.md`，将本次工作拆为后端确定性、图事件一致性、异常显性化、前端流程/UI、验证五段
- 约束：不创建新 worktree；行为修改按 TDD 思路先补测试
- 已完成后端请求契约、图事件、显性异常、SSE error 终止、相关测试修复；后端测试曾验证为 `98 passed`
- 已补前端回归测试覆盖：表单不再提交 `days`、并行阶段包含天气、错误不被 `done` 覆盖、状态图不误推断天气完成、生成后自动跳转结果页
- 已重做日期选择为日历范围选择 + 天数步进器 + 快捷天数；表单增加人数步进和旅行节奏
- 已修正规划状态图为 3 个并行节点和拓扑感知状态推导，并减少规划页卡片堆叠
- 已更新 Playwright 用例，覆盖请求不带 `days`、完成后自动跳转结果页、移动端首页可用
- 已通过 `npm run build`、`npm run test:unit`、`npm run test:e2e`
- 已通过后端 `DEBUG=false uv run pytest -q`，结果 `98 passed`
- 已启动前端开发服务器：`http://127.0.0.1:5174/`
- 已修复景点 POI 搜索规则过度限制：LLM 可执行关键词优先搜索，`types` 只作为可选精确过滤；非 110 的博物馆/展馆/街区类 POI 不再被景点节点丢弃
- 已为上述问题补回归测试，并通过后端全量测试 `98 passed`
- 已修复移动端规划完成页详情面板无法触摸滚动的问题；新增 Playwright 回归用例，验证 sheet 内容区可通过触摸上滑滚动
- 已重新构建前端并通过 `npm run test:e2e`（3 passed）与 `npm run test:unit`（27 passed）
- 已重做首页为地图工具式紧凑表单，移除输入框内部原生边框/焦点轮廓导致的黑框和文字裁切问题
- 已重做结果页为地图优先布局：地图全屏铺底，左侧为浮动信息面板，移除全屏雾化遮罩、地图渐变遮罩和默认覆盖地图的 Current Day/景点大卡
- 已补 Playwright 回归测试覆盖：普通目的地文本不裁切、输入框无原生边框、结果页跳转后滚动归零、结果页无全屏雾化层、桌面侧栏不超过 400px
- 已重排结果页侧栏顶部：侧栏改为实底白色，操作按钮改为图标化工具栏，标签独立成行，当前选中 Day 摘要前置到日程切换上方，避免被切换列表和顶部标签挤到后面
- 已补 Playwright 回归测试覆盖：侧栏实底、返回按钮不换行、当前 Day 摘要位于日程切换前方
- 已通过前端 `npm run lint`、`npm run build`、`npm run test:unit`（27 passed）和 `npm run test:e2e`（6 passed）
- 已重做规划过程图为卡片式 SVG 拓扑画布：节点文字收进固定尺寸节点卡片，连线只连接节点边界，分支和汇聚走独立通道，不再让文字、节点和箭头互相覆盖
- 已补 `PlanningFlow` 单测，验证画布节点、边数量、当前节点高亮和分支边高亮；真实浏览器检查 10 个 workflow 节点边界无交叠
- 已通过前端 `npm run lint`、`npm run build`、`npm run test:unit`（28 passed）和 `npm run test:e2e`（6 passed）
- 已按用户反馈改掉直角线：工作流边全部改为三次贝塞尔曲线，增加白色底描边和 18px 大箭头；节点整体缩小并拉大层距，保证箭头段可见
- 已补单测约束所有 `data-workflow-edge` 必须为曲线；真实浏览器检查节点无重叠，主链路相邻层保留 55px 以上空隙
- 已再次通过前端 `npm run lint`、`npm run build`、`npm run test:unit`（28 passed）和 `npm run test:e2e`（6 passed）
- 已修正规划过程图手绘曲线过度歪扭的问题：主链路改为近垂直轻微弯曲，分支保留干净弧线；线宽降为 3/4，箭头放大到 24px
- 已补 `PlanningFlow` 单测约束大箭头和主链路横向摆动范围，并通过 `npm run test:unit -- PlanningFlow.test.ts`
- 已通过前端 `npm run lint`、`npm run build`、`npm run test:unit`（28 passed）和 `npm run test:e2e`（6 passed）
- 已用真实浏览器检查规划流程图：10 个节点无重叠，11 条边正常渲染，活动线 4px、底描边 9px、箭头 24px，主链路单段横向摆动 12px
- 已修复成稿完成后没有自动进入结果页/没有查看按钮的问题：后端改为先验证并发送 `itinerary`，再发送 `final_planning agent_done`；缺失最终行程时直接报错，不再显示假完成
- 已增强规划页结果入口：只要 `itinerary` 已到达，即使自动跳转还没完成，也显示“查看行程”按钮，点击会保存并进入结果页
- 已补后端回归测试覆盖最终行程事件顺序和缺失 `itinerary_draft` 不标记完成；已补前端回归测试覆盖 itinerary 已存在时必须保留手动入口
- 已通过后端 `DEBUG=false uv run pytest -q`（100 passed）和前端 `npm run lint`、`npm run build`、`npm run test:unit`（29 passed）、`npm run test:e2e`（6 passed）
- 已修复最终成稿阶段 `LLM response did not contain a valid JSON object`：通用 `invoke_llm_json_async` 现在和其他 JSON Agent 一样启用 `response_format=json_object`，不再让总结文案调用随机返回普通文本
- 已补 `test_llm_utils` 回归测试，验证通用 JSON LLM 调用确实传入 JSON 输出模式；真实流式请求已收到 `itinerary -> final_planning agent_done -> done`
- 已通过后端 `DEBUG=false uv run pytest -q`（101 passed）和前端 Playwright e2e（6 passed）
- 已修复前端规划图在最终阶段卡住的问题：收到 `itinerary` 时同步把 `final_planning` agent 标为 completed，避免后续 `agent_done` 未赶上跳转或快照保存时显示“最终成稿执行中”
- 已补 planner store 回归测试，并通过前端 `npm run lint`、`npm run build`、`npm run test:unit`（29 passed）、`npm run test:e2e`（6 passed）；真实浏览器请求已自动进入 `/plan/...` 结果页
- 已修复点击“查看行程”时 `localStorage` 超配额导致路由中断的问题：保存结果前压缩 plan/planningState，删除重复 itinerary 和图片字段，历史记录限制为最近 5 条，并在 quota 触发时逐步淘汰旧记录
- 已补 storage 单测覆盖“规划快照不重复保存 itinerary”和“quota 触发时淘汰旧记录”；已补 Playwright e2e 覆盖旧行程记录过大时仍能进入结果页且不冒泡 `QuotaExceededError`
- 已通过前端 `npm run test:unit`（31 passed）、`npm run build`、`npm run lint`、`npm run test:e2e`（7 passed）
- 已继续放宽搜索策略：景点多关键词改为逐个独立查询，并取消“候选数量够就自动停”的早停；酒店/餐饮默认不再传规则推导的 `types`，只有用户显式提供 type 限制时才作为高德过滤条件
- 已补清洗层约束：放宽召回后，酒店节点会拒绝非住宿 POI，餐饮节点会拒绝非餐饮 POI，避免无 `types` 时混入景点/酒店等错误结果
- 已补后端回归测试覆盖上述行为，并通过 `DEBUG=false uv run pytest -q`（106 passed）
